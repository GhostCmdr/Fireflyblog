---
title: 编辑功能测试文章
published: 2026-08-31
description: 测试草稿箱云端草稿
category: 测试
tags: [编辑, 测试, 功能验证]
image: ./images/1.png
password: 123456
passwordHint: 示例文章密码123456
pinned: true
draft: true
---


# 测试云端草稿

这是一篇专门用于验证「编辑此文」按钮 → 编辑器载入 → 发布更新全链路的测试文章。下面尽量覆盖所有元数据与正文元素。

## 一、元数据字段说明（本文章已填写）

| 字段 | 值 | 用途 |
|------|------|------|
| title | 编辑功能测试文章 | 标题 |
| slug | edit-test | 自定义 URL（英文） |
| published | 2026-08-20 | 发布日期（编辑后应保留） |
| draft | false | 非草稿 |
| description | 用于验证… | 描述 |
| category | 测试 | 分类 |
| tags | 编辑, 测试, 功能验证 | 标签 |
| image | /images/test-cover.jpg | 封面图 |
| pinned | true | 置顶 |
| comment | true | 允许评论 |

## 二、各类代码块测试

### 1. Markdown 行内与基础

行内代码：`const x = 1`，加粗 **重点**，斜体 *强调*，链接 [Firefly](https://github.com/GhostCmdr/Fireflyblog)。

- 无序列表项 A
- 无序列表项 B
  - 嵌套项 B-1
  - 嵌套项 B-2
1. 有序项一
2. 有序项二

> 引用块：编辑文章后这段内容应原样保留。

### 2. JavaScript

```javascript
function greet(name) {
  const msg = `Hello, ${name}!`;
  console.log(msg);
  return msg;
}
greet('Firefly');
```

### 3. TypeScript

```typescript
interface Post {
  title: string;
  slug: string;
  published: string;
}

function buildFrontmatter(opts: Post): string {
  return `---\ntitle: ${opts.title}\nslug: ${opts.slug}\n---`;
}
```

### 4. Bash

```bash
pnpm dev
astro build
git add . && git commit -m "test" && git push
```

### 5. JSON

```json
{
  "title": "编辑功能测试文章",
  "slug": "edit-test",
  "tags": ["编辑", "测试", "功能验证"],
  "pinned": true
}
```

### 6. HTML

```html
<article>
  <h1>标题</h1>
  <p>正文内容</p>
</article>
```

### 7. CSS

```css
.edit-this-post-btn {
  display: none;
}
body.editor-enabled .edit-this-post-btn {
  display: inline-flex;
}
```

### 8. Astro 组件片段

```astro
---
const { title } = Astro.props;
---
<a href={`/editor/?slug=${encodeURIComponent(title)}`}>编辑此文</a>
```

### 9. 带特殊字符的代码（引号与数组）

```javascript
const arr = ["a", "b", 'c'];
const obj = { key: "value with \"quotes\" inside" };
```

## 三、图片测试

封面图字段已设 `image: ./images/1.png`（桌面 1.png）。正文插图如下：

### 1. 单行图片（2.png）


### 2. 并列图片（grid 块，3.png + 4.png）


## 四、结论

如果通过「编辑此文」进入编辑器后，上面所有字段、代码块、表格、图片都能正确载入并显示，且发布后 GitHub 仓库文件内容一致、published 日期不变，则编辑功能验证通过。
